{% extends "NfqNomNomBundle:Default:index.html.twig" %}
{% block content %}
    <script>
        $(document).ready(function() {
            $("#addrecipetoevent_recipe").val(["AL","AZ"]).select2();
            $("#addrecipetoevent_recipe").select2({width: 'resolve'});
            $("#addrecipetoevent_recipe").click(function() { $("#addrecipetoevent_recipe").select2(); });
            $("#wait_tags_destroy").click(function() { $("#wait_").select2("destroy");
            });
        });
    </script>

    {{ error }}
    {{ form_start(forma, {'attr': {'class': 'well span6'}}) }}
        {{ form_row(forma.recipe) }}
        {{ form_row(forma.submit, {'attr': {'class': 'btn btn-primary'}}) }}
    {{ form_end(forma) }}
    {#generated content displaying#}
{% endblock %}