{% extends "NfqNomNomBundle:Default:index.html.twig" %}
{% block content %}
    <script>
        var object = "#adduserstoevent_user";
        $(document).ready(function() {
            addGenemuFunctionality(object);
        });
    </script>

    {{ error }}
    {{ form_start(forma, {'attr': {'class': 'well'}}) }}
        {{ form_row(forma.user) }}
        {{ form_row(forma.submit, {'attr': {'class': 'btn btn-primary'}}) }}
    {{ form_end(forma) }}
    {#generated content displaying#}
{% endblock %}